apiVersion: v1
kind: Pod
metadata:
  name: failure-simulation-pod
  namespace: monitoring
  labels:
    app: failure-simulation
spec:
  containers:
  - name: main-container
    image: busybox
    command: ["/bin/sh", "-c", "echo 'Simulating failure'; exit 1"]
